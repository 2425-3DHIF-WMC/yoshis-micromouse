function huntAndKill(width: number, height: number) : number[][]{

    width -= width % 2;
    width++;
    height -= height % 2;
    height++;

    let maze: number[][] = [];
    //TO_DO: Implement Hunt & Kill algorithm
    return maze;
}